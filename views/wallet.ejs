<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
  <title>BlueCrypto | Tensor Network</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">

  <style>
    :root {
      color-scheme: light;
      --bg1: #e9f8ff; --bg2: #f8fcff; --bg3: #d7f0ff;
      --glass: rgba(255,255,255,0.85); --glass2: rgba(255,255,255,0.65);
      --solid: #ffffff; --soft: #f0f7ff;
      --text: #0f172a; --muted: #64748b; --muted2: #94a3b8;
      --blue: #0284c7; --blue2: #2563eb; --cyan: #38bdf8;
      --pink: #e11d48; --orange: #d97706; --green: #10b981;
      --tensor: #8b5cf6; --tensor2: #d946ef; 
      --border: rgba(2,132,199,0.12); --border2: rgba(2,132,199,0.35);
      --shadow: 0 24px 64px rgba(37,99,235,0.12); --shadow2: 0 12px 32px rgba(2,132,199,0.08);
      --safe-top: env(safe-area-inset-top); --safe-bottom: env(safe-area-inset-bottom);
    }

    [data-theme="dark"] {
      color-scheme: dark;
      --bg1: #06111f; --bg2: #050816; --bg3: #0a2942;
      --glass: rgba(13,22,40,0.88); --glass2: rgba(17,29,51,0.72);
      --solid: #0d1628; --soft: #111d33;
      --text: #f8fafc; --muted: #94a3b8; --muted2: #64748b;
      --blue: #38bdf8; --tensor: #a78bfa; --tensor2: #e879f9;
      --border: rgba(125,211,252,0.16); --border2: rgba(56,189,248,0.36);
      --shadow: 0 28px 86px rgba(0,0,0,0.5); --shadow2: 0 14px 38px rgba(0,0,0,0.3);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; font-family: Inter, system-ui, sans-serif; -webkit-tap-highlight-color: transparent; }
    html, body { width: 100%; min-height: 100vh; overflow-x: hidden; scroll-behavior: smooth; }
    body {
      color: var(--text);
      background: radial-gradient(circle at 10% 0%, rgba(56,189,248,0.25), transparent 35%), radial-gradient(circle at 90% 15%, rgba(37,99,235,0.15), transparent 30%), radial-gradient(circle at 50% 100%, rgba(14,165,233,0.2), transparent 40%), linear-gradient(135deg, var(--bg1), var(--bg2) 50%, var(--bg3));
      background-attachment: fixed; padding-bottom: calc(92px + var(--safe-bottom)); -webkit-font-smoothing: antialiased;
    }
    button, input, select, textarea { font: inherit; outline: none; }
    button { border: 0; cursor: pointer; transition: transform 0.1s ease, filter 0.1s ease; }
    button:active { transform: scale(0.97); filter: brightness(0.95); }

    .topbar { position: sticky; top: 0; z-index: 50; padding: calc(10px + var(--safe-top)) 12px 10px; background: rgba(248,251,255,0.7); border-bottom: 1px solid var(--border); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px); }
    [data-theme="dark"] .topbar { background: rgba(5,8,22,0.8); }
    .topbar-inner { width: min(980px, 100%); margin: 0 auto; display: flex; align-items: center; justify-content: space-between; }
    .brand { display: flex; align-items: center; gap: 12px; text-decoration: none; color: inherit; }
    .logo { width: 46px; height: 46px; border-radius: 18px; display: grid; place-items: center; color: white; font-weight: 900; font-size: 1.2rem; background: linear-gradient(135deg, var(--blue2), var(--cyan)); box-shadow: 0 12px 28px rgba(37,99,235,0.25); }
    .brand-title { font-weight: 900; letter-spacing: -0.04em; font-size: 1.1rem; }
    .brand-sub { color: var(--muted); font-size: 0.75rem; font-weight: 700; margin-top: 2px; }
    .top-actions { display: flex; gap: 8px; }
    .icon-btn { width: 44px; height: 44px; border-radius: 16px; background: var(--glass); border: 1px solid var(--border); color: var(--text); box-shadow: var(--shadow2); backdrop-filter: blur(16px); font-weight: 900; display: grid; place-items: center; }

    .shell { width: min(980px, 100%); margin: 0 auto; padding: 12px; }
    .hero { position: relative; overflow: hidden; border-radius: 32px; padding: 24px; background: var(--glass); border: 1px solid var(--border); box-shadow: var(--shadow); backdrop-filter: blur(32px); -webkit-backdrop-filter: blur(32px); }
    .hero::before { content: ""; position: absolute; right: -80px; bottom: -100px; width: 280px; height: 280px; border-radius: 50%; background: radial-gradient(circle, rgba(2,132,199,0.2), transparent 60%); pointer-events: none; }
    
    .badge { display: inline-flex; align-items: center; gap: 6px; padding: 8px 12px; border-radius: 99px; color: var(--blue); background: rgba(2,132,199,0.1); border: 1px solid var(--border2); font-size: 0.75rem; font-weight: 800; margin-bottom: 8px; }
    .dot { width: 8px; height: 8px; border-radius: 50%; background: var(--blue); animation: pulse 2s infinite; }
    @keyframes pulse { 0% { box-shadow: 0 0 0 0 rgba(2,132,199,0.5); } 70% { box-shadow: 0 0 0 8px rgba(2,132,199,0); } 100% { box-shadow: 0 0 0 0 rgba(2,132,199,0); } }
    
    .balance { font-size: clamp(2.8rem, 12vw, 5.5rem); font-weight: 900; line-height: 1; letter-spacing: -0.05em; font-variant-numeric: tabular-nums; margin-top: 4px; }
    .pnl-line { margin-top: 12px; display: inline-flex; align-items: center; gap: 8px; padding: 8px 14px; border-radius: 99px; background: var(--glass2); border: 1px solid var(--border); color: var(--muted); font-weight: 800; font-size: 0.85rem; }
    .pnl-line.up { color: var(--blue); background: rgba(2,132,199,0.08); border-color: rgba(2,132,199,0.15); }
    .pnl-line.down { color: var(--pink); background: rgba(225,29,72,0.08); border-color: rgba(225,29,72,0.15); }

    .market-strip { margin-top: 24px; display: flex; gap: 12px; overflow-x: auto; scroll-snap-type: x proximity; scrollbar-width: none; padding-bottom: 8px; }
    .market-strip::-webkit-scrollbar { display: none; }
    .market-card { flex: 0 0 140px; scroll-snap-align: start; padding: 12px; border-radius: 20px; background: var(--glass2); border: 1px solid var(--border); box-shadow: var(--shadow2); color: var(--text); text-align: left; }
    
    .quick { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-top: 24px; }
    .quick-btn { min-height: 76px; border-radius: 24px; padding: 12px; text-align: center; color: var(--text); background: var(--solid); border: 1px solid var(--border); box-shadow: var(--shadow2); display: flex; flex-direction: column; align-items: center; justify-content: center; }
    .quick-btn.primary { color: white; background: linear-gradient(135deg, var(--blue2), var(--cyan)); border: none; box-shadow: 0 16px 32px rgba(37,99,235,0.25); }
    .quick-btn.tensor-btn { color: white; background: linear-gradient(135deg, var(--tensor), var(--tensor2)); border: none; box-shadow: 0 16px 32px rgba(139,92,246,0.25); }
    .quick-btn strong { font-size: 0.95rem; font-weight: 900; margin-bottom: 4px; }
    .quick-btn span { color: var(--muted); font-size: 0.7rem; font-weight: 700; }
    .quick-btn.primary span, .quick-btn.tensor-btn span { color: rgba(255,255,255,0.9); }

    .panel { margin-top: 14px; border-radius: 28px; padding: 20px; background: var(--glass); border: 1px solid var(--border); box-shadow: var(--shadow2); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px); }
    .panel.tensor-panel { border-color: rgba(139,92,246,0.3); background: linear-gradient(180deg, rgba(139,92,246,0.05), var(--glass)); }
    .panel-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
    .title { font-size: 1.25rem; font-weight: 900; letter-spacing: -0.03em; }

    .asset-scroll { display: flex; gap: 12px; overflow-x: auto; scroll-snap-type: x proximity; scrollbar-width: none; padding-bottom: 6px; }
    .asset-scroll::-webkit-scrollbar { display: none; }
    .modal-asset-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); gap: 10px; max-height: 240px; overflow-y: auto; padding-bottom: 6px; }
    
    .asset-card { flex: 0 0 190px; scroll-snap-align: start; border-radius: 24px; padding: 16px; background: var(--glass2); border: 1px solid var(--border); color: var(--text); text-align: left; box-shadow: var(--shadow2); transition: 0.15s ease; position: relative; }
    .asset-card:active { transform: scale(0.97); }
    .asset-card.tensor { border-color: rgba(139,92,246,0.4); background: rgba(139,92,246,0.05); }
    .asset-card.watch { background: linear-gradient(180deg, var(--glass2), rgba(255,255,255,0.3)); }
    [data-theme="dark"] .asset-card.watch { background: linear-gradient(180deg, var(--glass2), rgba(17,29,51,0.4)); }
    .asset-card.selected, .receive-option.selected { border-color: var(--cyan); box-shadow: 0 0 0 4px rgba(56,189,248,0.2); }
    .asset-card.tensor.selected { border-color: var(--tensor); box-shadow: 0 0 0 4px rgba(139,92,246,0.2); }

    .asset-top { display: flex; align-items: center; gap: 12px; }
    .coin { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 50%; background: var(--solid); border: 1px solid var(--border); color: var(--blue); font-weight: 900; font-size: 0.75rem; flex-shrink: 0; padding: 4px; overflow: hidden; }
    .coin.tensor-coin { border: none; background: transparent; padding: 0; }
    .coin.tensor-coin svg { width: 44px; height: 44px; }
    .coin img { width: 100%; height: 100%; object-fit: contain; }

    .name { font-weight: 900; font-size: 1rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .tiny { color: var(--muted); font-size: 0.75rem; font-weight: 700; margin-top: 2px; }
    .asset-value { margin-top: 14px; font-size: 1.25rem; font-weight: 900; font-variant-numeric: tabular-nums; }
    .asset-sub { margin-top: 6px; color: var(--muted); font-size: 0.75rem; font-weight: 700; display:flex; align-items:center; gap:6px; flex-wrap:wrap; }

    .small-btn { height: 38px; padding: 0 14px; font-size: 0.85rem; border-radius: 16px; font-weight: 800; background: var(--solid); border: 1px solid var(--border); color: var(--text); }
    .primary-btn { width: 100%; height: 52px; border-radius: 20px; font-size: 0.95rem; font-weight: 900; color: white; background: linear-gradient(135deg, var(--blue2), var(--cyan)); box-shadow: 0 12px 24px rgba(37,99,235,0.25); }
    .tensor-primary-btn { width: 100%; height: 52px; border-radius: 20px; font-size: 0.95rem; font-weight: 900; color: white; background: linear-gradient(135deg, var(--tensor), var(--tensor2)); box-shadow: 0 12px 24px rgba(139,92,246,0.25); }
    .secondary-btn { width: 100%; height: 52px; border-radius: 20px; font-size: 0.95rem; font-weight: 800; background: var(--solid); border: 1px solid var(--border); color: var(--text); }

    .form { display: grid; gap: 12px; }
    .field { display: grid; gap: 8px; }
    label { color: var(--muted); font-size: 0.75rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.05em; }

    .input, .select { width: 100%; height: 54px; padding: 0 16px; border-radius: 18px; background: var(--soft); border: 1px solid var(--border); color: var(--text); font-weight: 800; font-size: 0.95rem; transition: border-color 0.2s; }
    .input:focus, .select:focus { border-color: var(--cyan); }
    .select { appearance: none; background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2.5' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e"); background-repeat: no-repeat; background-position: right 16px center; background-size: 18px; padding-right: 48px; }

    .box { padding: 18px; border-radius: 24px; background: var(--glass2); border: 1px solid var(--border); }
    .box.tensor { background: rgba(139,92,246,0.05); border-color: rgba(139,92,246,0.2); }
    .box.bad { color: var(--pink); background: rgba(225,29,72,0.06); border-color: rgba(225,29,72,0.15); }

    .icon-picker-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 8px; margin-top: 8px; }
    .icon-option { height: 48px; border-radius: 14px; border: 1px solid var(--border); background: var(--solid); display: grid; place-items: center; color: var(--muted); transition: 0.2s; }
    .icon-option.selected { border-color: var(--tensor); box-shadow: 0 0 0 3px rgba(139,92,246,0.2); background: rgba(139,92,246,0.05); }
    .icon-option svg { width: 32px; height: 32px; }

    .receive-hero { display: flex; flex-direction: column; align-items: center; background: var(--solid); border-radius: 28px; padding: 24px; border: 1px solid var(--border); margin-bottom: 16px; box-shadow: var(--shadow2); }
    .receive-address-box { width: 100%; display: flex; align-items: center; gap: 8px; background: var(--soft); padding: 8px 8px 8px 16px; border-radius: 20px; border: 1px solid var(--border); }
    .receive-address-box input { flex: 1; background: transparent; border: none; font-size: 0.85rem; color: var(--text); font-family: monospace; font-weight: 700; }
    
    .huge-amount { font-size: 3rem; font-weight: 900; text-align: center; width: 100%; border: none; background: transparent; color: var(--text); padding: 10px 0; outline: none; margin-top: 10px; font-variant-numeric: tabular-nums; }
    .send-max-btn { background: var(--soft); color: var(--blue); border-radius: 99px; padding: 6px 16px; font-size: 0.75rem; font-weight: 800; border: 1px solid var(--border); display: inline-block; margin: 0 auto; margin-bottom: 20px; }
    .send-top-wrap { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 10px 0; }

    .modal-overlay, .drawer-overlay { position: fixed; inset: 0; z-index: 100; display: none; background: rgba(15,23,42,0.6); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); padding: 16px; overflow-y: auto; }
    .modal-overlay.active, .drawer-overlay.active { display: block; }
    .modal-card { width: min(500px, 100%); margin: calc(12px + var(--safe-top)) auto 20px auto; padding: 24px; border-radius: 32px; background: var(--solid); border: 1px solid var(--border); box-shadow: var(--shadow); animation: pop 0.25s cubic-bezier(0.2, 0.9, 0.3, 1.1) both; }
    .drawer { position: absolute; top: 0; right: 0; width: min(440px, 100%); height: 100%; overflow-y: auto; padding: calc(24px + var(--safe-top)) 24px calc(24px + var(--safe-bottom)); background: var(--solid); border-left: 1px solid var(--border); box-shadow: -20px 0 80px rgba(0,0,0,0.2); animation: slide 0.25s ease both; }

    @keyframes pop { from { transform: translateY(20px) scale(0.95); opacity: 0; } to { transform: translateY(0) scale(1); opacity: 1; } }
    @keyframes slide { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }

    .modal-head, .drawer-head { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 24px; }
    .modal-title, .drawer-title { font-size: 1.4rem; font-weight: 900; letter-spacing: -0.04em; }
    .close { width: 44px; height: 44px; border-radius: 18px; background: var(--soft); border: 1px solid var(--border); font-size: 1.4rem; font-weight: 900; color: var(--text); }

    .toast { position: fixed; left: 50%; bottom: calc(100px + var(--safe-bottom)); transform: translateX(-50%) translateY(20px); z-index: 150; padding: 14px 24px; border-radius: 20px; background: rgba(15,23,42,0.95); color: white; font-weight: 800; font-size: 0.9rem; box-shadow: 0 16px 32px rgba(0,0,0,0.3); opacity: 0; pointer-events: none; transition: all 0.3s cubic-bezier(0.2, 0.9, 0.3, 1.1); white-space:nowrap; }
    .toast.active { opacity: 1; transform: translateX(-50%) translateY(0); }

    .bottom-nav { position: fixed; left: 50%; bottom: calc(12px + var(--safe-bottom)); transform: translateX(-50%); z-index: 70; width: min(440px, calc(100% - 24px)); display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; padding: 8px; border-radius: 28px; background: rgba(255,255,255,0.92); border: 1px solid var(--border); box-shadow: var(--shadow); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px); }
    [data-theme="dark"] .bottom-nav { background: rgba(13,22,40,0.95); }
    .bottom-nav button { min-height: 58px; border-radius: 20px; color: var(--muted); background: transparent; font-size: 0.75rem; font-weight: 800; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:4px; }
    .bottom-nav button svg { width: 22px; height: 22px; stroke: currentColor; fill: none; stroke-width: 2.5; stroke-linecap: round; stroke-linejoin: round; transition:0.2s; }
    .bottom-nav button.active { color: var(--blue); background: var(--soft); box-shadow: inset 0 0 0 1px var(--border); }
    .bottom-nav button.active svg { stroke: var(--blue); }

    @media (max-width: 580px) {
      .hero { padding: 20px; border-radius: 28px; }
      .quick { grid-template-columns: repeat(4, 1fr); gap: 6px; }
      .quick-btn { min-height: 72px; padding: 10px 4px; border-radius: 20px; }
      .quick-btn strong { font-size: 0.8rem; }
      .panel { padding: 16px; border-radius: 24px; }
      .asset-card { flex-basis: 170px; }
      .modal-card { width: 100%; border-radius: 32px 32px 0 0; margin: auto auto 0 auto; max-height: 92vh; }
      .drawer { width: 100%; }
      .huge-amount { font-size: 2.5rem; }
    }
  </style>
</head>

<body>
  <header class="topbar">
    <div class="topbar-inner">
      <a href="/" class="brand">
        <div class="logo">B</div>
        <div>
          <div class="brand-title">BlueCrypto</div>
          <div class="brand-sub" id="statusText">Connecting...</div>
        </div>
      </a>
      <div class="top-actions">
        <button class="icon-btn" onclick="openSettings()">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
        </button>
      </div>
    </div>
  </header>

  <main class="shell">
    <section class="hero" id="homeSection">
      <div class="hero-inner">
        <div class="badge"><span class="dot"></span><span id="walletStatusBadge">Connecting...</span></div>
        <div class="balance" id="totalUsd">$0.00</div>
        
        <div class="market-strip" id="topMarketCards" style="margin-top:20px; margin-bottom: 8px;"></div>
        
        <div class="quick">
          <button class="quick-btn tensor-btn" onclick="openModal('tensorSwapModal')"><strong>Tensor</strong><span>AI Swap</span></button>
          <button class="quick-btn primary" onclick="openModal('receiveModal')"><strong>Receive</strong><span>Scan QR</span></button>
          <button class="quick-btn" onclick="openModal('sendModal')"><strong>Send</strong><span>Transfer</span></button>
          <button class="quick-btn" onclick="openModal('swapModal')"><strong>DEX</strong><span>Trade</span></button>
        </div>
      </div>
    </section>

    <section class="panel" id="assetsSection">
      <div class="panel-head"><h2 class="title">Crypto Assets</h2><button class="small-btn" onclick="refreshAllBalances(true)">Refresh</button></div>
      <div class="asset-scroll" id="assetCards"></div>
    </section>

    <section class="panel tensor-panel" id="tensorAssetsSection">
      <div class="panel-head"><h2 class="title" style="color:var(--tensor);">Tensor Assets (AI)</h2><button class="small-btn" style="color:var(--tensor); border-color:var(--tensor);" onclick="openModal('tensorSendModal')">Send</button></div>
      <div class="asset-scroll" id="tensorCards"></div>
    </section>
  </main>

  <nav class="bottom-nav">
    <button id="navHome" class="active" onclick="window.scrollTo({top: 0, behavior: 'smooth'}); navSetActive('navHome');"><svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>Home</button>
    <button id="navTrading" onclick="window.location.href='trading'"><svg viewBox="0 0 24 24"><path d="M3 3v18h18"/><path d="M18.7 8l-5.1 5.2-2.8-2.7L7 14.3"/></svg>Trading</button>
    <button id="navSettings" onclick="openSettings()"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>Settings</button>
  </nav>

  <div class="modal-overlay" id="tensorChartModal" onclick="overlayClose(event,'tensorChartModal')">
    <div class="modal-card" style="width:min(640px, 100%); border-color:var(--tensor);">
      <div class="modal-head">
        <div>
          <div class="modal-title" id="chartModalTitle" style="color:var(--tensor);">AI Token Dynamics</div>
          <div class="modal-sub" id="chartModalStats">Cap: $0.00 | Dominance: 0.00%</div>
        </div>
        <button class="close" onclick="closeModal('tensorChartModal')">×</button>
      </div>
      <div style="position:relative; width:100%; height:260px; background:var(--soft); border-radius:18px; padding:10px; overflow:hidden;">
         <canvas id="tensorCanvasInterface" style="width:100%; height:100%; display:block;"></canvas>
      </div>
      <div style="display:grid; grid-template-columns:repeat(3, 1fr); gap:10px; margin-top:14px; text-align:center;">
         <div class="box" style="padding:10px;"><div class="tiny">Circulating Supply</div><strong id="chartMetricSupply" style="font-size:0.85rem;">0</strong></div>
         <div class="box" style="padding:10px;"><div class="tiny">24h Vol (Simulated)</div><strong id="chartMetricVolume" style="font-size:0.85rem;">$0.00</strong></div>
         <div class="box" style="padding:10px;"><div class="tiny">Dominance Rate</div><strong id="chartMetricDominance" style="font-size:0.85rem; color:var(--tensor);">0.00%</strong></div>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="tensorManageModal" onclick="overlayClose(event,'tensorManageModal')">
    <div class="modal-card" style="border-color:var(--tensor);">
      <div class="modal-head">
        <div>
          <div class="modal-title" style="color:var(--tensor);">Manage AI Asset</div>
          <div class="modal-sub" id="manageTokenName">Update token mechanics.</div>
        </div>
        <button class="close" onclick="closeModal('tensorManageModal')">×</button>
      </div>
      <div class="form">
        <input type="hidden" id="editTokenId" />
        <div class="field">
          <label>Algorithm Bias</label>
          <select class="select" id="editBias">
            <option value="bull">Bullish Bias</option>
            <option value="bear">Bearish Bias</option>
            <option value="random">Random Walk</option>
            <option value="pegged">True Stablecoin</option>
          </select>
        </div>
        <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px;">
          <div class="field"><label>Min Interval Drop/Raise (%)</label><input class="input" id="editMinPct" type="number" step="0.1" /></div>
          <div class="field"><label>Max Interval Drop/Raise (%)</label><input class="input" id="editMaxPct" type="number" step="0.1" /></div>
        </div>
        <div class="field"><label>Circulating Supply</label><input class="input" id="editSupply" type="number" step="1" /></div>
        
        <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:10px;">
          <button class="secondary-btn" style="color:var(--pink); border-color:var(--pink);" onclick="deleteTensorToken()">Delete Asset</button>
          <button class="tensor-primary-btn" onclick="updateTensorToken()">Save Changes</button>
        </div>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="tensorSwapModal" onclick="overlayClose(event,'tensorSwapModal')">
    <div class="modal-card" style="border-color:var(--tensor);">
      <div class="modal-head"><div><div class="modal-title" style="color:var(--tensor);">Tensor AI Swap</div><div class="modal-sub">Acquire AI tokens via USDT burn.</div></div><button class="close" onclick="closeModal('tensorSwapModal')">×</button></div>
      <div class="form">
        <div class="box tensor">
          <div style="display:flex; justify-content:space-between;">
            <label>Burn USDT</label>
            <div class="tiny" style="color:var(--pink);">Fee: 0.0001%</div>
          </div>
          <div class="tiny" id="tenSwapUsdtBal" style="margin-bottom:8px;">0.00 available</div>
          <input class="input" id="tenSwapAmountIn" type="number" step="any" placeholder="0.00" oninput="calcTensorOutput()" style="background:var(--solid);" />
        </div>
        <div style="display:grid; place-items:center; height:24px; margin:-8px 0; position:relative; z-index:2;"><div style="width:40px; height:40px; background:var(--solid); border:1px solid var(--tensor); border-radius:50%; display:grid; place-items:center; color:var(--tensor); font-weight:900;">↓</div></div>
        <div class="box tensor"><label>Select Tensor Token</label><select class="select" id="tenSwapSelectOut" onchange="calcTensorOutput()" style="background:var(--solid); margin-bottom:8px;"></select><div class="tiny">Est. Receive: <span id="tenSwapReceiveTxt" style="font-weight:800; color:var(--tensor);">0.00</span></div></div>
        <button class="tensor-primary-btn" onclick="executeTensorSwap()">Execute AI Swap</button>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="tensorSendModal" onclick="overlayClose(event,'tensorSendModal')">
    <div class="modal-card" style="border-color:var(--tensor);">
      <div class="modal-head"><div><div class="modal-title" style="color:var(--tensor);">Send Tensor Asset</div><div class="modal-sub">Transfer via T0x network.</div></div><button class="close" onclick="closeModal('tensorSendModal')">×</button></div>
      <div class="form">
        <div class="field"><label>Asset</label><select class="select" id="tenSendSelect" onchange="updateTenSendBal()"></select></div>
        <div class="send-top-wrap"><input class="huge-amount" id="tenSendAmt" type="number" step="any" placeholder="0.00" /><button class="send-max-btn" style="color:var(--tensor); border-color:var(--tensor);" onclick="maxTenSend()">Max</button><div class="tiny" id="tenSendBalTxt">0.00 available</div></div>
        <div class="field"><label>Recipient (T0x...)</label><input class="input" id="tenSendTo" placeholder="T0x..." /></div>
        <button class="tensor-primary-btn" onclick="executeTensorSend()">Send AI Token</button>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="receiveModal" onclick="overlayClose(event,'receiveModal')">
    <div class="modal-card">
      <div class="modal-head"><div><div class="modal-title">Receive</div><div class="modal-sub">Crypto and Tensor networks.</div></div><button class="close" onclick="closeModal('receiveModal')">×</button></div>
      <div class="form">
        <div class="receive-hero">
          <div class="qr-wrapper"><div id="qrBox"></div><div class="qr-badge" id="qrNetworkBadge">ETH</div></div>
          <div class="name" id="receiveTitle" style="margin-bottom:12px; font-size:1.15rem;">Select Asset</div>
          <div class="receive-address-box"><input id="receiveAddressInput" readonly onclick="this.select(); copyReceiveAddress();" /><button class="small-btn" style="background:var(--blue); color:white; border:none; height:36px;" id="copyAddrBtn" onclick="copyReceiveAddress()">Copy</button></div>
        </div>
        <div class="field"><label>Network Group</label><select class="select" id="receiveGroupSelect" onchange="renderReceiveAssets()"><option value="crypto">Standard Crypto</option><option value="tensor">Tensor Network (AI)</option></select></div>
        <div class="field" id="recNetWrap"><label>Chain</label><select class="select" id="receiveNetworkSelect" onchange="renderReceiveAssets()"></select></div>
        <label>Available Assets</label><div class="form" id="receiveAssetsList" style="max-height: 220px; overflow-y: auto; padding-right: 4px;"></div>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="sendModal" onclick="overlayClose(event,'sendModal')">
    <div class="modal-card">
      <div class="modal-head"><div><div class="modal-title">Send Crypto</div><div class="modal-sub">Fast & secure transfers.</div></div><button class="close" onclick="closeModal('sendModal')">×</button></div>
      <div class="form">
        <label>Asset to Send</label><div class="modal-asset-grid" id="sendOwnedAssets" style="justify-content:center; margin-bottom:10px;"></div>
        <div class="send-top-wrap"><input class="huge-amount" id="sendAmountInput" type="number" step="any" placeholder="0.00" oninput="scheduleImmediateFeeRefresh()" /><button class="send-max-btn" onclick="setMaxSend()">Max Available</button><div class="tiny" id="sendBalanceText" style="margin-bottom:8px;">Select an asset.</div></div>
        <div class="field"><label>Recipient Address</label><input class="input" id="sendToInput" placeholder="0x..." autocomplete="off" oninput="scheduleImmediateFeeRefresh()" /></div>
        <div class="box" style="display:flex; align-items:center; justify-content:space-between; padding:14px 18px;"><div><div class="name" style="font-size:0.9rem;">Est. Network Fee</div><div class="tiny" id="sendFeePreview">Enter address and amount.</div></div><div class="countdown-ring" id="sendCountdown" style="--progress:1"><span>5</span></div></div>
        <button class="primary-btn" onclick="openSendConfirm()" style="margin-top:8px;">Review Send</button>
      </div>
    </div>
  </div>
  
  <div class="modal-overlay" id="sendConfirmModal" onclick="overlayClose(event,'sendConfirmModal')">
    <div class="modal-card">
      <div class="modal-head"><div><div class="modal-title">Confirm Send</div></div><button class="close" onclick="closeModal('sendConfirmModal')">×</button></div>
      <div class="box bad"><div class="name">Final review</div><div class="tiny" id="sendConfirmText"></div></div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:24px;"><button class="secondary-btn" onclick="closeModal('sendConfirmModal')">Cancel</button><button class="primary-btn" onclick="sendRealTransaction()">Confirm Send</button></div>
    </div>
  </div>

  <div class="modal-overlay" id="swapModal" onclick="overlayClose(event,'swapModal')">
    <div class="modal-card">
      <div class="modal-head"><div><div class="modal-title">DEX Swap</div><div class="modal-sub">On-chain decentralized routing.</div></div><button class="close" onclick="closeModal('swapModal')">×</button></div>
      <div class="form">
        <div class="box"><label>You Sell</label><div class="modal-asset-grid" id="swapOwnedAssets" style="margin-top:12px;"></div><div class="tiny" id="swapSellBalanceText" style="margin-top:12px; text-align:right;">Select an asset</div><input class="input" id="swapAmount" type="number" step="any" placeholder="0.00" style="margin-top:10px; border:none; background:var(--solid);" oninput="scheduleImmediateFeeRefresh()" /></div>
        <div style="display:grid; place-items:center; height:24px; margin:-8px 0; position:relative; z-index:2;"><div style="width:40px; height:40px; background:var(--solid); border:1px solid var(--border); border-radius:50%; display:grid; place-items:center; color:var(--blue); font-weight:900;">↓</div></div>
        <div class="box"><label>You Receive</label><select class="select" id="swapBuySelect" style="margin-top:12px; border:none; background:var(--solid);" onchange="scheduleImmediateFeeRefresh()"></select></div>
        <div class="box" style="display:flex; align-items:center; justify-content:space-between;"><div><div class="name">Estimated Output</div><div class="tiny" id="swapQuoteText">Enter amount to fetch route.</div></div><div class="countdown-ring" id="swapCountdown" style="--progress:1"><span>5</span></div></div>
        <button class="primary-btn" onclick="openSwapConfirm()">Review Swap</button>
      </div>
    </div>
  </div>

  <div class="drawer-overlay" id="settingsOverlay" onclick="overlayClose(event,'settingsOverlay')">
    <aside class="drawer">
      <div class="drawer-head"><div><div class="drawer-title">Settings</div><div class="drawer-sub">Preferences and security.</div></div><button class="close" onclick="closeSettings()">×</button></div>
      <div class="form">
        <div class="box"><div class="name">Web3 Address</div><div class="tiny" id="settingsAddress" style="font-family:monospace; margin-top:6px; font-size:0.8rem; word-break:break-all;">Loading...</div></div>
        <div class="box tensor"><div class="name" style="color:var(--tensor);">Tensor Address (T0x)</div><div class="tiny" id="settingsTensorAddress" style="font-family:monospace; margin-top:6px; font-size:0.8rem; word-break:break-all;">Loading...</div></div>
        
        <div id="adminSettingsControls" style="display:none; margin-top:16px;">
           <div class="box bad" style="border-color:var(--cyan); background:rgba(6,182,212,0.05);">
             <div class="name" style="color:var(--cyan);">Admin: Demo Balances</div>
             <div class="field" style="margin-top:8px;"><label>ETH Balance</label><input class="input" id="adBalETH" type="number" step="any" /></div>
             <div class="field"><label>USDT Balance</label><input class="input" id="adBalUSDT" type="number" step="any" /></div>
             <div class="field"><label>BTC Balance</label><input class="input" id="adBalBTC" type="number" step="any" /></div>
             <button class="primary-btn" style="margin-top:12px; height:44px;" onclick="saveAdminBalances()">Save Crypto Balances</button>
           </div>
           
           <div class="box tensor" style="margin-top:12px;">
             <div class="name" style="color:var(--tensor);">Admin: Tensor Factory</div>
             <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:8px;">
               <div class="field"><label>Name</label><input class="input" id="adTenName" placeholder="Neural" /></div>
               <div class="field"><label>Symbol</label><input class="input" id="adTenSym" placeholder="NRL" /></div>
             </div>
             <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:8px;">
               <div class="field"><label>Price ($)</label><input class="input" id="adTenPrice" type="number" step="any" placeholder="1.00" /></div>
               <div class="field">
                 <label>Bias</label>
                 <select class="select" id="adTenBias">
                   <option value="bull">Bullish</option>
                   <option value="bear">Bearish</option>
                   <option value="random">Random</option>
                   <option value="pegged">True Stablecoin</option>
                 </select>
               </div>
             </div>
             <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px; margin-top:8px;">
               <div class="field"><label>Drop/Raise Min %</label><input class="input" id="adTenMinPct" type="number" value="1" step="0.1" /></div>
               <div class="field"><label>Drop/Raise Max %</label><input class="input" id="adTenMaxPct" type="number" value="5" step="0.1" /></div>
             </div>
             <div class="field" style="margin-top:8px;"><label>Total Circulating Supply</label><input class="input" id="adTenSupply" type="number" value="10000000" /></div>
             <div class="field" style="margin-top:8px;"><label>Icon Setup</label><div class="icon-picker-grid" id="adTenIconPicker"></div></div>
             <button class="tensor-primary-btn" style="margin-top:12px; height:44px;" onclick="deployTensorToken()">Deploy AI Token</button>
           </div>
        </div>

        <div class="field" style="margin-top:12px;"><label>Display currency</label><select class="select" id="currencySelect" onchange="setDisplayCurrency(this.value)"><option value="USD">USD</option><option value="AED">AED</option><option value="EUR">EUR</option><option value="GBP">GBP</option><option value="SAR">SAR</option></select></div>
        <div class="field"><label>Theme</label><div style="display:grid; grid-template-columns:1fr 1fr; gap:12px;"><button class="secondary-btn" style="height:48px;" onclick="setTheme('light')">Light</button><button class="secondary-btn" style="height:48px;" onclick="setTheme('dark')">Dark</button></div></div>
        
        <div class="box bad" id="securityBox">
          <div class="name" style="color:var(--pink);">Security & Recovery</div>
          <div class="tiny" style="margin-bottom:12px;">Backup keys before wiping data.</div>
          <div style="display:grid; grid-template-columns:1fr 1fr; gap:12px;">
            <button class="secondary-btn" style="height:44px; font-size:0.85rem;" onclick="showSeed()">Web3 Seed</button>
            <button class="secondary-btn" style="height:44px; font-size:0.85rem;" onclick="showTensorSeed()">Tensor Seed</button>
          </div>
          <textarea class="input" id="secretOutput" readonly style="margin-top:12px; height:90px; font-size:0.8rem; line-height:1.4; filter:blur(5px); transition:0.3s; cursor:pointer;" onclick="this.style.filter='none'; this.select()"></textarea>
        </div>
        <form action="/logout" method="POST" style="margin-top:6px;"><button class="secondary-btn" style="height:48px;" type="submit">Logout</button></form>
      </div>
    </aside>
  </div>

  <div class="toast" id="toast"></div>

  <script src="https://cdn.jsdelivr.net/npm/ethers@6.13.5/dist/ethers.umd.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/qrcode@1.5.4/build/qrcode.min.js"></script>

  <script>
    const SERVER_WALLET = <%- typeof wallet !== 'undefined' ? wallet : 'null' %>;
    const CURRENT_EMAIL = "<%= typeof email !== 'undefined' ? email : '' %>";
    const USER_ROLE = "<%= typeof role !== 'undefined' ? role : 'user' %>";
    const IS_ADMIN = USER_ROLE === 'staff';

    const DISPLAY_RATES = { USD:{symbol:'$',rate:1}, AED:{symbol:'AED ',rate:3.6725}, EUR:{symbol:'€',rate:.92}, GBP:{symbol:'£',rate:.79}, SAR:{symbol:'SAR ',rate:3.75} };
    
    // Expanded Networks including BTC and SOL for receive/display purposes
    const NETWORKS = { 
      ethereum:{name:'Ethereum',chainId:1,symbol:'ETH'}, 
      bsc:{name:'BNB Chain',chainId:56,symbol:'BNB'}, 
      polygon:{name:'Polygon',chainId:137,symbol:'MATIC'},
      bitcoin:{name:'Bitcoin Native',chainId:null,symbol:'BTC'},
      solana:{name:'Solana Native',chainId:null,symbol:'SOL'}
    };
    
    // Assets that appear on the receive list natively per chain selection
    const CHAIN_ASSETS = {
      ethereum: [{symbol:'ETH', name:'Ethereum'}, {symbol:'USDT', name:'Tether'}, {symbol:'USDC', name:'USD Coin'}],
      bsc: [{symbol:'BNB', name:'BNB'}, {symbol:'USDT', name:'Tether'}],
      polygon: [{symbol:'MATIC', name:'Polygon'}, {symbol:'USDT', name:'Tether'}, {symbol:'USDC', name:'USD Coin'}],
      bitcoin: [{symbol:'BTC', name:'Bitcoin'}],
      solana: [{symbol:'SOL', name:'Solana'}, {symbol:'USDC', name:'USD Coin'}]
    };

    const TENSOR_ICONS = [
      `<svg viewBox="0 0 24 24" fill="url(#grad1)"><defs><linearGradient id="grad1" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" stop-color="#ec4899" /><stop offset="100%" stop-color="#8b5cf6" /></linearGradient></defs><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>`,
      `<svg viewBox="0 0 24 24" fill="#0ea5e9"><circle cx="12" cy="12" r="10"/><path fill="#ffffff" d="M12 6a6 6 0 1 0 0 12 6 6 0 0 0 0-12z"/></svg>`,
      `<svg viewBox="0 0 24 24" fill="#10b981"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path stroke="#ffffff" stroke-width="2" d="M12 8v4l3 3"/></svg>`,
      `<svg viewBox="0 0 24 24" stroke="#f59e0b" stroke-width="2" fill="none"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>`,
      `<svg viewBox="0 0 24 24" fill="#8b5cf6"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line stroke="#ffffff" stroke-width="2" x1="8" y1="21" x2="16" y2="21"/><line stroke="#ffffff" stroke-width="2" x1="12" y1="17" x2="12" y2="21"/></svg>`,
      `<svg viewBox="0 0 24 24" fill="#f43f5e"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>`,
      `<svg viewBox="0 0 24 24" fill="#84cc16"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>`
    ];

    const WATCH_ASSETS = [{symbol:'BTC',name:'Bitcoin',gecko:'bitcoin'}, {symbol:'ETH',name:'Ethereum',gecko:'ethereum'}, {symbol:'SOL',name:'Solana',gecko:'solana'}];
    const TOP_5_STRIP = ['bitcoin', 'ethereum', 'solana', 'binancecoin', 'ripple']; 
    
    let displayCurrency = localStorage.getItem('blue_display_currency') || 'USD';
    let activeSigner = null, allBalances = [], combinedAssets = [], ownedPositions = [];
    let tensorAddress = "", tensorRegistry = [], tensorBalances = {};
    let prices = {};
    let toastTimer;

    // Charting Engine
    let chartingIntervalObject = null;
    let activeChartTokenId = null;

    // TENSOR LOGIC 
    async function initTensor() {
      tensorAddress = SERVER_WALLET?.tensorAddress || "T0x...Loading";
      
      // Ensure Tensor Seed Phrase exists
      if (!SERVER_WALLET?.tensorVault) {
         try {
           const randWallet = ethers.Wallet.createRandom();
           const phrase = randWallet.mnemonic.phrase;
           await fetch('/api/tensor/vault', { 
               method: 'POST', 
               headers: {'Content-Type': 'application/json'}, 
               body: JSON.stringify({ tensorVault: phrase })
           });
           SERVER_WALLET.tensorVault = phrase;
         } catch(e) { console.error("Tensor Vault Sync Failed", e); }
      }

      if (IS_ADMIN) {
        document.getElementById('adminSettingsControls').style.display = 'block';
        document.getElementById('adTenIconPicker').innerHTML = TENSOR_ICONS.map((svg, i) => `<button class="icon-option ${i===0?'selected':''}" onclick="selAdTenIcon(this, ${i})">${svg}</button>`).join('');
        window.selectedTenIcon = 0;
        
        const saved = JSON.parse(localStorage.getItem('blue_admin_balances')) || {eth: 5.4, usdt: 25000, btc: 0.38};
        document.getElementById('adBalETH').value = saved.eth;
        document.getElementById('adBalUSDT').value = saved.usdt;
        document.getElementById('adBalBTC').value = saved.btc;
      }
      
      await fetchTensorData();
      setInterval(fetchTensorData, 8000);
    }
    
    function selAdTenIcon(btn, i) { document.querySelectorAll('.icon-option').forEach(b=>b.classList.remove('selected')); btn.classList.add('selected'); window.selectedTenIcon = i; }
    
    function saveAdminBalances() {
      const eth = Number(document.getElementById('adBalETH').value);
      const usdt = Number(document.getElementById('adBalUSDT').value);
      const btc = Number(document.getElementById('adBalBTC').value);
      localStorage.setItem('blue_admin_balances', JSON.stringify({eth, usdt, btc}));
      toast('Demo crypto balances saved!');
      refreshAllBalances(true);
    }

    async function fetchTensorData() {
       try {
         const res = await fetch('/api/tensor');
         if(res.ok) {
            const data = await res.json();
            tensorRegistry = data.registry || [];
            tensorAddress = data.address || tensorAddress;
            tensorBalances = data.balances || {};
         }
       } catch(e) { console.error(e); }
       
       renderTensorCards();
       if(document.getElementById('tensorSwapModal').classList.contains('active')) calcTensorOutput();
    }

    async function deployTensorToken() {
      const name = document.getElementById('adTenName').value; 
      const symbol = document.getElementById('adTenSym').value.toUpperCase();
      const price = Number(document.getElementById('adTenPrice').value); 
      const bias = document.getElementById('adTenBias').value;
      const supply = Number(document.getElementById('adTenSupply').value);
      const minPct = Number(document.getElementById('adTenMinPct').value);
      const maxPct = Number(document.getElementById('adTenMaxPct').value);

      if(!name || !symbol || price <= 0 || supply <= 0 || minPct < 0 || maxPct < 0) return toast('Invalid details');
      
      try {
        const res = await fetch('/api/tensor/deploy', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ name, symbol, price, bias, minPct, maxPct, icon: TENSOR_ICONS[window.selectedTenIcon], supply })
        });
        if(res.ok) { toast(`${symbol} deployed globally!`); fetchTensorData(); } 
        else throw new Error('Failed');
      } catch (e) { toast('Deployment failed.'); }
      closeSettings();
    }

    // --- Admin Tensor Management CRUD ---
    function openTensorManage(id) {
      const t = tensorRegistry.find(x => x.id === id);
      if(!t) return;
      document.getElementById('editTokenId').value = id;
      document.getElementById('manageTokenName').textContent = `Editing ${t.symbol}`;
      document.getElementById('editBias').value = t.bias || 'random';
      document.getElementById('editMinPct').value = (t.minPct || 0.01) * 100;
      document.getElementById('editMaxPct').value = (t.maxPct || 0.05) * 100;
      document.getElementById('editSupply').value = t.supply || 10000000;
      openModal('tensorManageModal');
    }

    async function updateTensorToken() {
      const id = document.getElementById('editTokenId').value;
      const bias = document.getElementById('editBias').value;
      const minPct = document.getElementById('editMinPct').value;
      const maxPct = document.getElementById('editMaxPct').value;
      const supply = document.getElementById('editSupply').value;

      try {
        const res = await fetch(`/api/tensor/update/${id}`, {
          method: 'PUT', headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({ bias, minPct, maxPct, supply })
        });
        if(res.ok) { toast('Token mechanics updated.'); closeModal('tensorManageModal'); fetchTensorData(); }
      } catch(e) { toast('Update failed.'); }
    }

    async function deleteTensorToken() {
      const id = document.getElementById('editTokenId').value;
      try {
        const res = await fetch(`/api/tensor/delete/${id}`, { method: 'DELETE' });
        if(res.ok) { toast('Token deleted.'); closeModal('tensorManageModal'); fetchTensorData(); }
      } catch(e) { toast('Deletion failed.'); }
    }

    function getUsdtBalanceTotal() { return ownedPositions.filter(p=>p.symbol==='USDT').reduce((acc,p)=>acc+Number(p.balance),0); }

    function openTensorSwap() {
      const s = document.getElementById('tenSwapSelectOut');
      s.innerHTML = tensorRegistry.map(t => `<option value="${t.id}">${t.symbol} - ${money(t.price)}</option>`).join('');
      document.getElementById('tenSwapUsdtBal').textContent = `${fmt(getUsdtBalanceTotal())} USDT available`;
      document.getElementById('tenSwapAmountIn').value = ''; document.getElementById('tenSwapReceiveTxt').textContent = '0.00';
    }

    function calcTensorOutput() {
      const amt = Number(document.getElementById('tenSwapAmountIn').value||0);
      const tId = document.getElementById('tenSwapSelectOut').value;
      const t = tensorRegistry.find(x=>x.id===tId);
      if(!t || amt<=0) { document.getElementById('tenSwapReceiveTxt').textContent = '0.00'; return; }
      
      const feeAmount = amt * 0.000001;
      const netAmount = amt - feeAmount;
      document.getElementById('tenSwapReceiveTxt').textContent = fmt(netAmount / t.price, 4) + ' ' + t.symbol;
    }

    async function executeTensorSwap() {
      const amt = Number(document.getElementById('tenSwapAmountIn').value||0);
      const tId = document.getElementById('tenSwapSelectOut').value;
      const t = tensorRegistry.find(x=>x.id===tId);
      if(!t || amt<=0) return toast('Invalid swap');
      
      const realUsdt = ownedPositions.find(p=>p.symbol==='USDT');
      if(IS_ADMIN || (realUsdt && realUsdt.balance >= amt)) {
        toast('Processing Swap...');
        try {
          const res = await fetch('/api/tensor/swap', {
             method: 'POST', headers: { 'Content-Type': 'application/json' },
             body: JSON.stringify({ tokenId: t.id, usdtAmount: amt })
          });
          const result = await res.json();
          if(res.ok) { 
             fetchTensorData(); closeModal('tensorSwapModal'); 
             return toast(`Received ${t.symbol}!`); 
          }
        } catch(e) { toast('Network error'); }
      } else { toast('Insufficient real USDT'); }
    }

    function updateTenSendBal() {
      const tId = document.getElementById('tenSendSelect').value;
      document.getElementById('tenSendBalTxt').textContent = `${fmt(tensorBalances[tId] || 0)} available`;
    }
    
    function maxTenSend() { document.getElementById('tenSendAmt').value = tensorBalances[document.getElementById('tenSendSelect').value] || 0; }
    
    async function executeTensorSend() {
      const tId = document.getElementById('tenSendSelect').value; const amt = Number(document.getElementById('tenSendAmt').value);
      const to = document.getElementById('tenSendTo').value.trim();
      if(!to.startsWith('T0x') || amt<=0 || (tensorBalances[tId]||0) < amt) return toast('Invalid details');
      
      toast('Processing T0x Tx...');
      try {
         const res = await fetch('/api/tensor/send', {
             method: 'POST', headers: { 'Content-Type': 'application/json' },
             body: JSON.stringify({ tokenId: tId, amount: amt, toAddress: to })
         });
         if(res.ok) { fetchTensorData(); closeModal('tensorSendModal'); return toast('AI Token Sent'); }
      } catch(e) { toast('Transfer failed.'); }
    }

    function renderTensorCards() {
      const box = document.getElementById('tensorCards');
      if(!tensorRegistry.length) { 
        box.innerHTML = `<div class="asset-card tensor"><div class="name">No Tensor Assets</div><div class="tiny">Admin must deploy tokens.</div></div>`; 
        return; 
      }
      
      let totTenUsd = 0;
      box.innerHTML = tensorRegistry.map(t => {
        const bal = tensorBalances[t.id] || 0; 
        const val = bal * (t.price || 0); 
        totTenUsd += val;
        const diff = t.startPrice ? (((t.price - t.startPrice)/t.startPrice)*100) : 0;
        const adminBtn = IS_ADMIN ? `<button class="small-btn" style="position:absolute; top:12px; right:12px; height:24px; padding:0 8px; font-size:0.65rem;" onclick="event.stopPropagation(); openTensorManage('${t.id}')">Manage</button>` : '';

        return `<div class="asset-card tensor" onclick="openTensorAnalytics('${t.id}')">
          ${adminBtn}
          <div class="asset-top">
            <div class="coin tensor-coin">${t.icon}</div>
            <div style="width:calc(100% - 56px); overflow:hidden;">
              <div class="name">${t.symbol}</div>
              <div class="tiny" style="color:var(--tensor); font-weight:800;">Dom: ${fmt(t.dominance || 0, 2)}%</div>
            </div>
          </div>
          <div class="asset-value">${money(t.price)}</div>
          <div class="asset-sub">
            <span class="${diff>=0?'pnl-line up':'pnl-line down'}" style="padding:2px 8px; font-size:0.7rem; margin:0; border:none; background:transparent;">${diff>=0?'+':''}${diff.toFixed(2)}%</span>
          </div>
          <div class="tiny" style="margin-top:8px; opacity:0.8; font-size:0.7rem;">
            Cap: $${formatCompact(t.marketCap || 0)} <br> Holds: ${fmt(bal,2)} ${t.symbol}
          </div>
        </div>`;
      }).join('');
      
      renderTotals(totTenUsd);
    }
    
    // TENSOR CHARTING 
    async function openTensorAnalytics(tokenId) {
      activeChartTokenId = tokenId;
      openModal('tensorChartModal');
      await fetchAndDrawChart();
      clearInterval(chartingIntervalObject);
      chartingIntervalObject = setInterval(fetchAndDrawChart, 5000);
    }

    async function fetchAndDrawChart() {
      if (!activeChartTokenId || !document.getElementById('tensorChartModal').classList.contains('active')) {
        clearInterval(chartingIntervalObject); return;
      }
      
      const token = tensorRegistry.find(t => t.id === activeChartTokenId);
      if (!token) return;

      document.getElementById('chartModalTitle').textContent = `${token.name} (${token.symbol}) Live Data`;
      document.getElementById('chartModalStats').textContent = `Cap: $${Number(token.marketCap || 0).toLocaleString(undefined, {maximumFractionDigits:0})} | Share: ${fmt(token.dominance || 0, 2)}%`;
      document.getElementById('chartMetricSupply').textContent = Number(token.supply || 0).toLocaleString();
      document.getElementById('chartMetricVolume').textContent = money(token.volume || 0);
      document.getElementById('chartMetricDominance').textContent = `${fmt(token.dominance || 0, 2)}%`;

      try {
        const response = await fetch(`/api/tensor/chart?tokenId=${activeChartTokenId}`);
        const data = await response.json();
        if (data && data.candles) drawCandlesticksOnCanvas(data.candles);
      } catch (e) { }
    }

    function drawCandlesticksOnCanvas(candles) {
      const canvas = document.getElementById('tensorCanvasInterface');
      if (!canvas) return;
      const ctx = canvas.getContext('2d');
      const rect = canvas.getBoundingClientRect();
      const dpr = window.devicePixelRatio || 1;
      
      canvas.width = rect.width * dpr;
      canvas.height = rect.height * dpr;
      ctx.scale(dpr, dpr);
      ctx.clearRect(0, 0, rect.width, rect.height);

      if (!candles || candles.length === 0) return;

      const paddingRight = 55, paddingTop = 15, paddingBottom = 15;
      const graphWidth = rect.width - paddingRight, graphHeight = rect.height - paddingTop - paddingBottom;
      const prices = candles.map(c => [c.high, c.low]).flat();
      let maxPrice = Math.max(...prices), minPrice = Math.min(...prices);
      if (maxPrice === minPrice) { maxPrice *= 1.05; minPrice *= 0.95; }
      const priceRange = maxPrice - minPrice || 1; 

      ctx.strokeStyle = 'rgba(148, 163, 184, 0.12)';
      ctx.fillStyle = '#94a3b8'; ctx.font = '10px monospace';
      
      for (let h = 0; h <= 4; h++) {
        const yPos = paddingTop + (graphHeight * (h/4));
        ctx.beginPath(); ctx.moveTo(0, yPos); ctx.lineTo(graphWidth, yPos); ctx.stroke();
        ctx.fillText((maxPrice - (priceRange * (h/4))).toFixed(4), graphWidth + 6, yPos + 3);
      }

      const totalBars = candles.length;
      const barWidth = Math.max(2, (graphWidth / totalBars) * 0.75), spaceWidth = (graphWidth / totalBars) * 0.25;

      candles.forEach((c, index) => {
        const xPos = (index * (graphWidth / totalBars)) + spaceWidth;
        const yOpen = paddingTop + graphHeight * (1 - ((c.open - minPrice) / priceRange));
        const yClose = paddingTop + graphHeight * (1 - ((c.close - minPrice) / priceRange));
        const yHigh = paddingTop + graphHeight * (1 - ((c.high - minPrice) / priceRange));
        const yLow = paddingTop + graphHeight * (1 - ((c.low - minPrice) / priceRange));

        const isBull = c.close >= c.open;
        ctx.strokeStyle = isBull ? '#10b981' : '#e11d48'; ctx.fillStyle = isBull ? '#10b981' : '#e11d48'; ctx.lineWidth = 1.5;

        ctx.beginPath(); ctx.moveTo(xPos + (barWidth / 2), yHigh); ctx.lineTo(xPos + (barWidth / 2), yLow); ctx.stroke();
        ctx.fillRect(xPos, Math.min(yOpen, yClose), barWidth, Math.max(1.5, Math.abs(yOpen - yClose)));
      });
    }

    // HELPER FUNCTIONS
    function toast(msg) { const el = document.getElementById('toast'); el.textContent = msg; el.classList.add('active'); clearTimeout(toastTimer); toastTimer = setTimeout(() => el.classList.remove('active'), 2500); }
    function formatCompact(num) { return !num ? '$0' : new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:'compact'}).format(num); }
    function money(val) { const cfg = DISPLAY_RATES[displayCurrency] || DISPLAY_RATES.USD; return `${cfg.symbol}${(Number(val||0) * cfg.rate).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`; }
    function fmt(val, dec=6) { return Number(val||0).toLocaleString(undefined,{maximumFractionDigits:dec}); }
    function icon(sym) { return `<div class="coin"><img src="https://assets.coincap.io/assets/icons/${String(sym).toLowerCase()}@2x.png" onerror="this.remove();this.parentNode.textContent='${String(sym).slice(0,4)}'" /></div>`; }

    function openModal(id) { document.getElementById(id).classList.add('active'); if(id==='tensorSwapModal') openTensorSwap(); if(id==='tensorSendModal') { document.getElementById('tenSendSelect').innerHTML = tensorRegistry.map(t=>`<option value="${t.id}">${t.symbol}</option>`).join(''); updateTenSendBal(); } if(id==='receiveModal') renderReceiveAssets(); }
    function closeModal(id) { document.getElementById(id).classList.remove('active'); if (id === 'tensorChartModal') clearInterval(chartingIntervalObject); }
    function overlayClose(e,id) { if(e.target.id === id) closeModal(id); }
    function navSetActive(id) { document.querySelectorAll('.bottom-nav button').forEach(b=>b.classList.remove('active')); document.getElementById(id)?.classList.add('active'); }
    
    function openSettings() { 
      document.getElementById('settingsOverlay').classList.add('active'); 
      document.getElementById('settingsAddress').textContent = IS_ADMIN ? "0xAdminDemo..." : (activeSigner ? activeSigner.address : "Loading..."); 
      document.getElementById('settingsTensorAddress').textContent = tensorAddress; 
      navSetActive('navSettings'); 
    }
    
    function closeSettings() { document.getElementById('settingsOverlay').classList.remove('active'); navSetActive('navHome'); }
    function setTheme(t) { document.documentElement.setAttribute('data-theme',t); localStorage.setItem('blue_theme',t); }
    function setDisplayCurrency(c) { displayCurrency = c; localStorage.setItem('blue_display_currency',c); document.getElementById('currencySelect').value = c; refreshAllBalances(false); }
    function priceFor(sym) { return prices[sym.toLowerCase()]?.usd || 0; }

    function combineAssets() {
      const map = new Map(); 
      WATCH_ASSETS.forEach(w => map.set(w.symbol, {symbol:w.symbol, name:w.name, gecko:w.gecko, amount:0, usd:0, positions:[], isWatch:true}));
      allBalances.forEach(a => {
        const sym = a.symbol; const usd = Number(a.balance||0)*priceFor(a.gecko);
        if(!map.has(sym)) map.set(sym, {symbol:sym, name:a.name, gecko:a.gecko, amount:0, usd:0, positions:[], isWatch:false});
        const item = map.get(sym); item.amount += Number(a.balance||0); item.usd += usd;
        if(Number(a.balance||0)>0) { item.isWatch=false; item.positions.push({...a,usd}); }
      });
      combinedAssets = [...map.values()].sort((a,b) => b.usd - a.usd);
      ownedPositions = allBalances.filter(x=>Number(x.balance||0)>0);
    }

    function renderTotals(tensorUsd = 0) {
      let tot = tensorUsd; combinedAssets.forEach(a => { tot += a.usd; });
      document.getElementById('totalUsd').textContent = money(tot);
    }

    function renderAssetCards() {
      document.getElementById('assetCards').innerHTML = combinedAssets.map(a => {
        return `<div class="asset-card ${a.usd<=0?'watch':''}">
          <div class="asset-top">${icon(a.symbol)}<div><div class="name">${a.symbol}</div><div class="tiny">${a.positions.length?`${a.positions.length} network(s)`:'Watchlist'}</div></div></div>
          <div class="asset-value">${a.usd>0?money(a.usd):money(priceFor(a.gecko))}</div>
          <div class="tiny" style="margin-top:8px; opacity:0.8;">${fmt(a.amount)} ${a.symbol}</div>
        </div>`;
      }).join('');
      renderTensorCards();
    }

    function renderMarketStrip() {
      const strip = document.getElementById('topMarketCards');
      strip.innerHTML = TOP_5_STRIP.map(gecko => {
        const p = prices[gecko]; if(!p) return '';
        const chg = p.usd_24h_change || 0;
        const sym = gecko === 'bitcoin' ? 'BTC' : gecko === 'ethereum' ? 'ETH' : gecko === 'solana' ? 'SOL' : gecko === 'binancecoin' ? 'BNB' : 'XRP';
        return `<div class="market-card">
           <div style="display:flex; align-items:center; gap:8px; margin-bottom:6px;">${icon(sym)} <strong>${sym}</strong></div>
           <div style="font-weight:900; font-size:1.1rem;">${money(p.usd)}</div>
           <div class="${chg>=0?'pnl-line up':'pnl-line down'}" style="margin-top:4px; padding:2px 6px; font-size:0.7rem;">${chg>=0?'+':''}${chg.toFixed(2)}%</div>
        </div>`;
      }).join('');
    }

    async function refreshAllBalances(force = false) {
      if(force) document.getElementById('statusText').textContent = 'Updating...';
      
      if(IS_ADMIN) { 
        const saved = JSON.parse(localStorage.getItem('blue_admin_balances')) || {eth: 5.4, usdt: 25000, btc: 0.38};
        allBalances = [
          {networkKey:'ethereum', type:'native', symbol:'ETH', name:'Ethereum', balance: saved.eth, decimals:18, gecko:'ethereum'},
          {networkKey:'ethereum', type:'erc20', symbol:'USDT', name:'Tether', balance: saved.usdt, decimals:6, gecko:'tether'},
          {networkKey:'bitcoin', type:'native', symbol:'BTC', name:'Bitcoin', balance: saved.btc, decimals:8, gecko:'bitcoin'}
        ]; 
        await fetchPricesForAll(); combineAssets(); renderAssetCards(); 
        document.getElementById('statusText').textContent = 'Connected'; document.getElementById('walletStatusBadge').textContent = 'Secured Wallet';
        return; 
      }
      
      try {
        await fetchPricesForAll();
        // Base user simulation
        allBalances = [{networkKey:'ethereum', type:'erc20', symbol:'USDT', name:'Tether', balance: 150.0, decimals:6, gecko:'tether'}];
        combineAssets(); renderAssetCards();
        document.getElementById('statusText').textContent = 'Connected'; document.getElementById('walletStatusBadge').textContent = 'Secured Wallet';
      } catch(e) {}
    }
    
    async function fetchPricesForAll() {
      try {
        const ids = [...TOP_5_STRIP, 'tether', 'usd-coin'];
        const res = await fetch(`/api/prices?ids=${ids.join(',')}`);
        prices = await res.json() || {};
        renderMarketStrip();
      } catch(e) {}
    }

    function renderReceiveAssets() {
      const g = document.getElementById('receiveGroupSelect').value;
      const nw = document.getElementById('recNetWrap');
      const box = document.getElementById('receiveAssetsList');
      
      if(g === 'tensor') {
        nw.style.display = 'none'; document.getElementById('qrNetworkBadge').textContent = 'T0x'; document.getElementById('receiveTitle').textContent = 'Tensor AI Token';
        document.getElementById('receiveAddressInput').value = tensorAddress; renderQr(tensorAddress);
        box.innerHTML = tensorRegistry.map(t=>`<button class="receive-option"><div style="display:flex; align-items:center; gap:12px;"><div class="coin tensor-coin">${t.icon}</div><div style="text-align:left;"><div class="name">${t.symbol}</div><div class="tiny">${t.name}</div></div></div></button>`).join('');
      } else {
        nw.style.display = 'block'; 
        const netKey = document.getElementById('receiveNetworkSelect').value || 'ethereum';
        const net = NETWORKS[netKey];
        document.getElementById('qrNetworkBadge').textContent = net.symbol; document.getElementById('receiveTitle').textContent = `Crypto on ${net.name}`;
        
        let addr = IS_ADMIN ? '0xAdminDemoWallet...' : (activeSigner?.address || 'No wallet');
        if (netKey === 'bitcoin') addr = 'bc1q' + Array.from(crypto.getRandomValues(new Uint8Array(18))).map(b=>b.toString(16).padStart(2,'0')).join('');
        if (netKey === 'solana') addr = Array.from(crypto.getRandomValues(new Uint8Array(22))).map(b=>b.toString(36)).join('');
        
        document.getElementById('receiveAddressInput').value = addr; renderQr(addr);
        
        const arr = CHAIN_ASSETS[netKey] || [];
        box.innerHTML = arr.map((a,i) => `<button class="receive-option ${i===0?'selected':''}"><div style="display:flex; align-items:center; gap:12px;">${icon(a.symbol)}<div><div class="name">${a.symbol}</div><div class="tiny">${a.name}</div></div></div></button>`).join('');
      }
    }

    function renderQr(val) { const box = document.getElementById('qrBox'); box.innerHTML = ''; if(val && window.QRCode) QRCode.toCanvas(val, {width:220, margin:1, color:{dark:'#0f172a',light:'#ffffff'}}, (err,cvs) => { if(!err) box.appendChild(cvs); }); }
    function copyReceiveAddress() { const a = document.getElementById('receiveAddressInput').value; navigator.clipboard?.writeText(a); toast('Address copied'); document.getElementById('copyAddrBtn').textContent='Copied!'; setTimeout(()=>document.getElementById('copyAddrBtn').textContent='Copy',2000); }

    function showSeed() { if(IS_ADMIN) return toast('Demo Mode.'); const el = document.getElementById('secretOutput'); el.value = SERVER_WALLET?.encryptedVault ? JSON.parse(atob(SERVER_WALLET.encryptedVault)).phrase : 'No Web3 Seed'; el.style.filter = 'none'; }
    function showTensorSeed() { if(IS_ADMIN) return toast('Demo Mode.'); const el = document.getElementById('secretOutput'); el.value = SERVER_WALLET?.tensorVault || 'No Tensor Seed Generated'; el.style.filter = 'none'; }

    async function init() {
      setTheme(localStorage.getItem('blue_theme')||'light'); setDisplayCurrency(displayCurrency); initTensor();
      const s = document.getElementById('receiveNetworkSelect'); if(s) s.innerHTML = Object.entries(NETWORKS).map(([k,n])=>`<option value="${k}">${n.name}</option>`).join('');
      
      if (IS_ADMIN) { await refreshAllBalances(true); return; }
      
      try {
        let savedVault = SERVER_WALLET?.encryptedVault;
        if (!savedVault && window.ethers) {
          const randomWallet = ethers.Wallet.createRandom();
          savedVault = btoa(JSON.stringify({ pk: randomWallet.privateKey, phrase: randomWallet.mnemonic.phrase }));
          await fetch('/api/wallet/vault', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ encryptedVault: savedVault, publicWallets: [randomWallet.address] }) });
        }
        if(savedVault) {
           const parsedVault = JSON.parse(atob(savedVault));
           activeSigner = { address: new ethers.Wallet(parsedVault.pk).address };
        }
        document.getElementById('walletStatusBadge').textContent = 'Secured Wallet'; await refreshAllBalances(true);
      } catch (err) {}
    }
    window.addEventListener('keydown', e => { if(e.key==='Escape') { closeSettings(); document.querySelectorAll('.modal-overlay').forEach(x=>x.classList.remove('active')); clearInterval(chartingIntervalObject); }});
    
    // Ensure scripts loaded before initializing
    window.onload = init;
  </script>
</body>
</html>
